import { OrderFormatPipe } from './order-format.pipe';

describe('OrderFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
